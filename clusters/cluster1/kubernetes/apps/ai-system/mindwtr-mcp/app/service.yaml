---
apiVersion: v1
kind: Service
metadata:
  name: mindwtr-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: mindwtr-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mindwtr-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
