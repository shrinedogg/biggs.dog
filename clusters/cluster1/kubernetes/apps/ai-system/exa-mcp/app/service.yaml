---
apiVersion: v1
kind: Service
metadata:
  name: exa-mcp
  namespace: ai-system
  labels:
    app.kubernetes.io/name: exa-mcp
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: exa-mcp
