apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: pocket-id-user-password
spec:
  externalSecretName: pocket-id-user-password
  namespaceSelectors:
  - matchLabels:
      kubernetes.io/metadata.name: cnpg-system
  - matchLabels:
      kubernetes.io/metadata.name: auth
  externalSecretSpec:
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      creationPolicy: Owner
    data:
    - secretKey: password
      remoteRef:
        key: "pocket-id-user-password"
        property: password
    - secretKey: username
      remoteRef:
        key: "pocket-id-user-password"
        property: username
