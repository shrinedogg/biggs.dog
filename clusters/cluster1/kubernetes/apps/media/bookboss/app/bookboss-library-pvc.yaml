apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookboss-library-pvc
  namespace: media
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
