apiVersion: v1
kind: Service
metadata:
  name: sable-service
  namespace: matrix
spec:
  type: ClusterIP
  selector:
    app: sable
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
