---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: fenrir
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/games-on-whales/fenrir
  ref:
    branch: main
    # Pinned for reproducibility; bump deliberately to pick up CRD changes.
    commit: ec6c640596aca12a1942dacb034790bcc96eb863
