apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: kagent-user-password
spec:
  externalSecretName: kagent-user-password
  namespaceSelectors:
  - matchLabels:
      kubernetes.io/metadata.name: cnpg-system
  - matchLabels:
      kubernetes.io/metadata.name: ai-system
  externalSecretSpec:
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      creationPolicy: Owner
    data:
    - secretKey: password
      remoteRef:
        key: "kagent-user-password"
        property: password
    - secretKey: username
      remoteRef:
        key: "kagent-user-password"
        property: username
