apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: affine-user-password
spec:
  externalSecretName: affine-user-password
  namespaceSelectors:
  - matchLabels:
      kubernetes.io/metadata.name: cnpg-system
  - matchLabels:
      kubernetes.io/metadata.name: affine
  externalSecretSpec:
    secretStoreRef:
      kind: ClusterSecretStore
      name: onepassword-connect
    target:
      creationPolicy: Owner
    data:
    - secretKey: password
      remoteRef:
        key: "affine-user-password"
        property: password
    - secretKey: username
      remoteRef:
        key: "affine-user-password"
        property: username
