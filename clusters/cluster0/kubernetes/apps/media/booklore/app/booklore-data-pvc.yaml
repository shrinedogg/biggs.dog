apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booklore-data-pvc
  namespace: media
  labels:
    app: booklore
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
  