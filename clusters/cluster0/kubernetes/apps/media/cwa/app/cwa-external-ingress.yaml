apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cwa
  namespace: media
  annotations:
    external-dns.alpha.kubernetes.io/hostname: biggs.dog
spec:
  ingressClassName: external
  rules:
  - host: calibre.biggs.dog
    http:
      paths:
      - path: /
        pathType: Prefix
        backend: 
          service:
            name: cwa-service
            port:
              number: 8083