apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cwa-plugins-pvc
  namespace: media
  labels:
    app: cwa
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
  