apiVersion: v1
kind: PersistentVolume
metadata:
  name: cwa-ingest-cache-pv
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Delete
  storageClassName: nfs
  mountOptions:
    - hard
    - noatime
    - nconnect=16
    - nfsvers=4.2
  nfs:
    path: /tank/library
    server: 192.168.2.30