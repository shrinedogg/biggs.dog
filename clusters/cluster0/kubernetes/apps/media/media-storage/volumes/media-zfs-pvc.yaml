apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-zfs-pvc
  namespace: media
spec:
  storageClassName: zfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 116Ti