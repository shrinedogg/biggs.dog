apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # The namespace ships with the cert-manager app dir (reconciled by the
  # Flux Kustomization in capi/infrastructure/flux-ks.yaml).
  - ./cert-manager/ks.yaml
