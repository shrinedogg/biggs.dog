apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: windrose
  namespace: games
spec:
  interval: 3m
  chart:
    spec:
      chart: windrose
      version: 1.2.1
      sourceRef:
        kind: HelmRepository
        name: windrose
        namespace: flux-system
  values:
    resources:
      limits:
        memory: 12Gi
      requests:
        memory: 12Gi
    windroseDedicatedServer:
      port: 28050
      volumeStorageSize: "5Gi"
      existingSecret: "windrose-secret"
    instances:
      - name: "The Bridge"
        password: ""
        inviteCode: "$$!1769!$$"
        maxPlayerCount: "4"
        userSelectedRegion: "EU"
        service:
          type: LoadBalancer
          externalTrafficPolicy: Cluster
          metadata:
            labels: {}
            annotations: {}
