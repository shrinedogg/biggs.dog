---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # INFRA TIER — roll out first (gateway edge, cert/secret controllers, flux)
  - ./infra-ks.yaml
  # APPS TIER — roll out second (dex, omni)
  - ./apps-ks.yaml
