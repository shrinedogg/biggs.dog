---
apiVersion: v1
kind: Service
metadata:
  name: nfs
  namespace: media
spec:
  type: ClusterIP
  clusterIP: 10.102.197.173  # Pinned IP for NFS PV references
  selector:
    app: nfs
  ports:
    - name: nfs-port
      port: 2049
      targetPort: 2049
      protocol: TCP
