---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nv-gpu-operator
  namespace: dreamcast
spec:
  interval: 3m
  chart:
    spec:
      chart: gpu-operator
      version: v26.3.3
      sourceRef:
        kind: HelmRepository
        name: nvidia
        namespace: flux-system
  values:
    nfd:
      enabled: true
    # Talos provides the driver via the nonfree-kmod-nvidia system extension.
    driver:
      enabled: false
    # Talos provides nvidia-container-toolkit via system extension + machine config;
    # the operator cannot mutate the immutable Talos host, so keep it disabled.
    toolkit:
      enabled: false
    operator:
      defaultRuntime: containerd
    # Time-slice the single physical GPU into 4 schedulable nvidia.com/gpu
    # replicas: fenrir session pods need one for the wolf sidecar and one for
    # the game container, so a single GPU is not enough without sharing.
    devicePlugin:
      config:
        create: true
        name: time-slicing-config
        default: time-slicing
        data:
          time-slicing: |-
            version: v1
            sharing:
              timeSlicing:
                resources:
                  - name: nvidia.com/gpu
                    replicas: 4
    # Custom DCGM-exporter metric profile. The image-builtin
    # dcp-metrics-included.csv (4.5.3-4.8.2-distroless) curates a subset that
    # OMITS DCGM_FI_DEV_XID_ERRORS -- the single most important signal for the
    # recurring nv-01 hard-freeze (suspected Xid 109 / CTX_SWITCH_TIMEOUT under
    # load; see nvidia-gpu-tuning). Without XID in the scrape, the freeze could
    # never be root-caused from metrics. We ship the canonical dcgm-exporter
    # profile (etc/dcp-metrics-included.csv) which restores XID_ERRORS and adds
    # remapped-rows / NVLink counters. ECC stays commented (consumer RTX 5090
    # has no ECC). The chart creates a ConfigMap (key dcgm-metrics.csv) and the
    # gpu-operator mounts it, pointing DCGM_EXPORTER_COLLECTORS at it.
    dcgmExporter:
      config:
        create: true
        name: dcgm-exporter-custom-metrics
        data: |-
          # Format
          # If line starts with a '#' it is considered a comment
          # DCGM FIELD, Prometheus metric type, help message

          # Clocks
          DCGM_FI_DEV_SM_CLOCK,  gauge, SM clock frequency (in MHz).
          DCGM_FI_DEV_MEM_CLOCK, gauge, Memory clock frequency (in MHz).

          # Temperature
          DCGM_FI_DEV_MEMORY_TEMP, gauge, Memory temperature (in C).
          DCGM_FI_DEV_GPU_TEMP,    gauge, GPU temperature (in C).

          # Power
          DCGM_FI_DEV_POWER_USAGE,              gauge, Power draw (in W).
          DCGM_FI_DEV_TOTAL_ENERGY_CONSUMPTION, counter, Total energy consumption since boot (in mJ).

          # PCIE
          DCGM_FI_PROF_PCIE_TX_BYTES,  counter, Total number of bytes transmitted through PCIe TX via NVML.
          DCGM_FI_PROF_PCIE_RX_BYTES,  counter, Total number of bytes received through PCIe RX via NVML.
          DCGM_FI_DEV_PCIE_REPLAY_COUNTER, counter, Total number of PCIe retries.

          # Utilization (the sample period varies depending on the product)
          DCGM_FI_DEV_GPU_UTIL,      gauge, GPU utilization (in %).
          DCGM_FI_DEV_MEM_COPY_UTIL, gauge, Memory utilization (in %).
          DCGM_FI_DEV_ENC_UTIL,      gauge, Encoder utilization (in %).
          DCGM_FI_DEV_DEC_UTIL,      gauge, Decoder utilization (in %).

          # Errors and violations
          DCGM_FI_DEV_XID_ERRORS,            gauge,   Value of the last XID error encountered.

          # Memory usage
          DCGM_FI_DEV_FB_FREE, gauge, Framebuffer memory free (in MiB).
          DCGM_FI_DEV_FB_USED, gauge, Framebuffer memory used (in MiB).
          DCGM_FI_DEV_FB_RESERVED, gauge, Framebuffer memory reserved (in MiB).

          # VGPU License status
          DCGM_FI_DEV_VGPU_LICENSE_STATUS, gauge, vGPU License status

          # Remapped rows
          DCGM_FI_DEV_UNCORRECTABLE_REMAPPED_ROWS, counter, Number of remapped rows for uncorrectable errors
          DCGM_FI_DEV_CORRECTABLE_REMAPPED_ROWS,   counter, Number of remapped rows for correctable errors
          DCGM_FI_DEV_ROW_REMAP_FAILURE,           gauge,   Whether remapping of rows has failed

          # Static configuration information. These appear as labels on the other metrics
          DCGM_FI_DRIVER_VERSION,        label, Driver Version

          # DCP metrics
          DCGM_FI_PROF_GR_ENGINE_ACTIVE,   gauge, Ratio of time the graphics engine is active.
          DCGM_FI_PROF_PIPE_TENSOR_ACTIVE, gauge, Ratio of cycles the tensor (HMMA) pipe is active.
          DCGM_FI_PROF_DRAM_ACTIVE,        gauge, Ratio of cycles the device memory interface is active sending or receiving data.
          DCGM_FI_PROF_PCIE_TX_BYTES,      gauge, The rate of data transmitted over the PCIe bus - including both protocol headers and data payloads - in bytes per second.
          DCGM_FI_PROF_PCIE_RX_BYTES,      gauge, The rate of data received over the PCIe bus - including both protocol headers and data payloads - in bytes per second.
