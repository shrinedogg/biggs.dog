---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  hostnames:
  - rook-ceph.biggs.dog
  parentRefs:
  - name: rook-ceph
  rules:
  - backendRefs:
    - name: rook-ceph-mgr-dashboard
      port: 7000
    matches:
    - path:
        type: PathPrefix
        value: /