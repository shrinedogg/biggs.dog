---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: oauth2-proxy
  namespace: auth
spec:
  interval: 3m
  chart:
    spec:
      chart: oauth2-proxy
      version: 10.7.0
      sourceRef:
        kind: HelmRepository
        name: oauth2-proxy
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    # client-id / client-secret / cookie-secret come from the oauth2-proxy
    # ExternalSecret (1Password item oauth2-proxy).
    config:
      existingSecret: oauth2-proxy
      configFile: |-
        provider = "oidc"
        provider_display_name = "Pocket ID"
        oidc_issuer_url = "https://id.biggs.dog"
        redirect_url = "https://auth.biggs.dog/oauth2/callback"
        # Auth-only forward-auth: return 202 to authenticated probes from the
        # gateway/other apps rather than proxying a real upstream.
        upstreams = ["static://202"]
        email_domains = ["*"]
        scope = "openid email profile groups"
        code_challenge_method = "S256"
        # Behind the agentgateway (TLS terminated upstream); share cookies across
        # every *.biggs.dog service so it can guard them.
        reverse_proxy = "true"
        cookie_domains = [".biggs.dog"]
        whitelist_domains = [".biggs.dog"]
        cookie_secure = "true"
        cookie_samesite = "lax"
        # Expose identity to protected upstreams via X-Auth-Request-* headers.
        set_xauthrequest = "true"
        pass_access_token = "true"
    # Cookie-based sessions (no Redis dependency).
    sessionStorage:
      type: cookie
    # The redis wait init container is only needed for redis session storage.
    initContainers:
      waitForRedis:
        enabled: false
    podDisruptionBudget:
      enabled: false
    resources:
      requests:
        cpu: 25m
        memory: 64Mi
      limits:
        memory: 128Mi
