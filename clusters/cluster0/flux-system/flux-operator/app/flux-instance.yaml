apiVersion: fluxcd.controlplane.io/v1
kind: FluxInstance
metadata:
  name: flux
  namespace: flux-system
  annotations:
    fluxcd.controlplane.io/reconcileEvery: "20m"
    fluxcd.controlplane.io/reconcileTimeout: "10m"
spec:
  distribution:
    version: "2.8.8"
    registry: "ghcr.io/fluxcd"
    artifact: "oci://ghcr.io/controlplaneio-fluxcd/flux-operator-manifests"
  components:
    - source-controller
    - kustomize-controller
    - helm-controller
    - notification-controller
  cluster:
    type: kubernetes
    size: medium
    multitenant: false
    networkPolicy: false
    domain: "cluster.local"
  kustomize:
    patches:
      - target:
          kind: Deployment
        patch: |
          - op: replace
            path: /spec/template/spec/nodeSelector
            value:
              kubernetes.io/os: linux
          - op: add
            path: /spec/template/spec/tolerations
            value:
              - key: "CriticalAddonsOnly"
                operator: "Exists"
      # Helm charts that use .Chart.Version in labels break when helm-controller
      # appends the artifact digest (e.g. 0.9.7+de7ad4a95185 or 0.1.0+d0c124d4a1fd).
      # Affects OCI and Git chart sources. layerSelector.operation=copy does not
      # stop this. Disable digest tracking so .Chart.Version stays Chart.yaml.
      - target:
          kind: Deployment
          name: helm-controller
        patch: |
          - op: add
            path: /spec/template/spec/containers/0/args/-
            value: --feature-gates=DisableChartDigestTracking=true
  sync:
    kind: GitRepository
    url: "https://github.com/shrinedogg/biggs.dog.git"
    ref: "refs/heads/main"
    path: "clusters/cluster0"
    provider: github
    pullSecret: "flux-system"
