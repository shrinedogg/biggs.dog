---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: kagent
  namespace: flux-system
spec:
  interval: 1h
  url: oci://ghcr.io/kagent-dev/kagent/helm/kagent
  ref:
    # When bumping this tag, also update KAGENT_VERSION in
    # ./clusters/cluster0/kubernetes/apps/kagent/kagent/ks.yaml (the HelmRelease
    # post-renderer uses it to sanitize the chart's image tags + version label).
    tag: "0.9.7-de7ad4a95185"
